function stimulus = selectStimulus(taskPhase,trial,options.task.inputs )
[~,J,V] = find(inputs(1,:))
switch taskPhase
    case 'first'
        [~,J,V] = find(inputs(1,:))
    case 'outcome'
        [~,J,V] = find(inputs(1,:))

end

end
